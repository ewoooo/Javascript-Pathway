// Importing the underscore library from node_modules
import _ from "underscore";

// Importing the underscore library from the current directory
// import _ from "./underscore";

const result = _.contains([1, 2, 3], 2);
console.log(result);
